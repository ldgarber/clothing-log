<%= form_for(outfit) do |f| %>
  <% if outfit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(outfit.errors.count, "error") %> prohibited this outfit from being saved:</h2>

      <ul>
      <% outfit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %>
    <%= f.text_field :date, data: { :provide => 'datepicker'}, value: Date.today.strftime('%m/%d/%Y') %>
  </div>
  <div class="field">
    <%= f.label :items %>
    <%= f.autocomplete_field :item_ids, autocomplete_item_name_outfits_path %>         
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
